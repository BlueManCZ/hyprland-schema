# AUTO-GENERATED by generate_schema.py, DO NOT EDIT
# Hyprland version: v0.54.3
# Generated: 2026-03-27T21:00:07Z
# Source: https://github.com/hyprwm/Hyprland/blob/v0.54.3/src/config/supplementary/ConfigDescriptions.hpp

from hyprland_schema._model import HyprOption

HYPRLAND_VERSION = "v0.54.3"


OPTIONS: tuple[HyprOption, ...] = (
    HyprOption(
        key="general:border_size",
        section=("general",),
        name="border_size",
        description="size of the border around windows",
        type="int",
        default=1,
        min=0,
        max=20,
    ),
    HyprOption(
        key="general:gaps_in",
        section=("general",),
        name="gaps_in",
        description=(
            "gaps between windows\n\nsupports css style gaps (top, right, bottom, left -> 5 10 "
            "15 20)"
        ),
        type="string",
        default="5",
    ),
    HyprOption(
        key="general:gaps_out",
        section=("general",),
        name="gaps_out",
        description=(
            "gaps between windows and monitor edges\n\nsupports css style gaps (top, right, bot"
            "tom, left -> 5 10 15 20)"
        ),
        type="string",
        default="20",
    ),
    HyprOption(
        key="general:float_gaps",
        section=("general",),
        name="float_gaps",
        description=(
            "gaps between windows and monitor edges for floating windows\n\nsupports css style "
            "gaps (top, right, bottom, left -> 5 10 15 20). \n-1 means default gaps_in/gaps_ou"
            "t\n0 means no gaps"
        ),
        type="string",
        default="0",
    ),
    HyprOption(
        key="general:gaps_workspaces",
        section=("general",),
        name="gaps_workspaces",
        description="gaps between workspaces. Stacks with gaps_out.",
        type="int",
        default=0,
        min=0,
        max=100,
    ),
    HyprOption(
        key="general:col.inactive_border",
        section=("general",),
        name="col.inactive_border",
        description="border color for inactive windows",
        type="gradient",
        default="0xff444444",
    ),
    HyprOption(
        key="general:col.active_border",
        section=("general",),
        name="col.active_border",
        description="border color for the active window",
        type="gradient",
        default="0xffffffff",
    ),
    HyprOption(
        key="general:col.nogroup_border",
        section=("general",),
        name="col.nogroup_border",
        description=(
            "inactive border color for window that cannot be added to a group (see denywindow"
            "fromgroup dispatcher)"
        ),
        type="gradient",
        default="0xffffaaff",
    ),
    HyprOption(
        key="general:col.nogroup_border_active",
        section=("general",),
        name="col.nogroup_border_active",
        description="active border color for window that cannot be added to a group",
        type="gradient",
        default="0xffff00ff",
    ),
    HyprOption(
        key="general:layout",
        section=("general",),
        name="layout",
        description="which layout to use. [dwindle/master]",
        type="string",
        default="dwindle",
    ),
    HyprOption(
        key="general:no_focus_fallback",
        section=("general",),
        name="no_focus_fallback",
        description=(
            "if true, will not fall back to the next available window when moving focus in a "
            "direction where no window was found"
        ),
        type="bool",
        default=False,
    ),
    HyprOption(
        key="general:resize_on_border",
        section=("general",),
        name="resize_on_border",
        description="enables resizing windows by clicking and dragging on borders and gaps",
        type="bool",
        default=False,
    ),
    HyprOption(
        key="general:extend_border_grab_area",
        section=("general",),
        name="extend_border_grab_area",
        description=(
            "extends the area around the border where you can click and drag on, only used wh"
            "en general:resize_on_border is on."
        ),
        type="int",
        default=15,
        min=0,
        max=100,
    ),
    HyprOption(
        key="general:hover_icon_on_border",
        section=("general",),
        name="hover_icon_on_border",
        description=(
            "show a cursor icon when hovering over borders, only used when general:resize_on_"
            "border is on."
        ),
        type="bool",
        default=True,
    ),
    HyprOption(
        key="general:allow_tearing",
        section=("general",),
        name="allow_tearing",
        description="master switch for allowing tearing to occur. See the Tearing page.",
        type="bool",
        default=False,
    ),
    HyprOption(
        key="general:resize_corner",
        section=("general",),
        name="resize_corner",
        description=(
            "force floating windows to use a specific corner when being resized (1-4 going cl"
            "ockwise from top left, 0 to disable)"
        ),
        type="int",
        default=0,
        min=0,
        max=4,
    ),
    HyprOption(
        key="general:snap:enabled",
        section=("general", "snap"),
        name="enabled",
        description="enable snapping for floating windows",
        type="bool",
        default=False,
    ),
    HyprOption(
        key="general:snap:window_gap",
        section=("general", "snap"),
        name="window_gap",
        description="minimum gap in pixels between windows before snapping",
        type="int",
        default=10,
        min=0,
        max=100,
    ),
    HyprOption(
        key="general:snap:monitor_gap",
        section=("general", "snap"),
        name="monitor_gap",
        description="minimum gap in pixels between window and monitor edges before snapping",
        type="int",
        default=10,
        min=0,
        max=100,
    ),
    HyprOption(
        key="general:snap:border_overlap",
        section=("general", "snap"),
        name="border_overlap",
        description=(
            "if true, windows snap such that only one border's worth of space is between them"
        ),
        type="bool",
        default=False,
    ),
    HyprOption(
        key="general:snap:respect_gaps",
        section=("general", "snap"),
        name="respect_gaps",
        description="if true, snapping will respect gaps between windows",
        type="bool",
        default=False,
    ),
    HyprOption(
        key="general:modal_parent_blocking",
        section=("general",),
        name="modal_parent_blocking",
        description="if true, parent windows of modals will not be interactive.",
        type="bool",
        default=True,
    ),
    HyprOption(
        key="general:locale",
        section=("general",),
        name="locale",
        description="overrides the system locale",
        type="string",
        default="",
    ),
    HyprOption(
        key="decoration:rounding",
        section=("decoration",),
        name="rounding",
        description="rounded corners' radius (in layout px)",
        type="int",
        default=0,
        min=0,
        max=20,
    ),
    HyprOption(
        key="decoration:rounding_power",
        section=("decoration",),
        name="rounding_power",
        description="rounding power of corners (2 is a circle)",
        type="float",
        default=2.0,
        min=2.0,
        max=10.0,
    ),
    HyprOption(
        key="decoration:active_opacity",
        section=("decoration",),
        name="active_opacity",
        description="opacity of active windows. [0.0 - 1.0]",
        type="float",
        default=1.0,
        min=0.0,
        max=1.0,
    ),
    HyprOption(
        key="decoration:inactive_opacity",
        section=("decoration",),
        name="inactive_opacity",
        description="opacity of inactive windows. [0.0 - 1.0]",
        type="float",
        default=1.0,
        min=0.0,
        max=1.0,
    ),
    HyprOption(
        key="decoration:fullscreen_opacity",
        section=("decoration",),
        name="fullscreen_opacity",
        description="opacity of fullscreen windows. [0.0 - 1.0]",
        type="float",
        default=1.0,
        min=0.0,
        max=1.0,
    ),
    HyprOption(
        key="decoration:shadow:enabled",
        section=("decoration", "shadow"),
        name="enabled",
        description="enable drop shadows on windows",
        type="bool",
        default=True,
    ),
    HyprOption(
        key="decoration:shadow:range",
        section=("decoration", "shadow"),
        name="range",
        description="Shadow range (size) in layout px",
        type="int",
        default=4,
        min=0,
        max=100,
    ),
    HyprOption(
        key="decoration:shadow:render_power",
        section=("decoration", "shadow"),
        name="render_power",
        description=(
            "in what power to render the falloff (more power, the faster the falloff) [1 - 4]"
        ),
        type="int",
        default=3,
        min=1,
        max=4,
    ),
    HyprOption(
        key="decoration:shadow:sharp",
        section=("decoration", "shadow"),
        name="sharp",
        description=(
            "whether the shadow should be sharp or not. Akin to an infinitely high render power."
        ),
        type="bool",
        default=False,
    ),
    HyprOption(
        key="decoration:shadow:ignore_window",
        section=("decoration", "shadow"),
        name="ignore_window",
        description=(
            "if true, the shadow will not be rendered behind the window itself, only around it."
        ),
        type="bool",
        default=True,
    ),
    HyprOption(
        key="decoration:shadow:color",
        section=("decoration", "shadow"),
        name="color",
        description="shadow's color. Alpha dictates shadow's opacity.",
        type="color",
        default="0xee1a1a1a",
    ),
    HyprOption(
        key="decoration:shadow:color_inactive",
        section=("decoration", "shadow"),
        name="color_inactive",
        description="inactive shadow color. (if not set, will fall back to col.shadow)",
        type="color",
        default=None,
    ),
    HyprOption(
        key="decoration:shadow:offset",
        section=("decoration", "shadow"),
        name="offset",
        description="shadow's rendering offset.",
        type="vec2",
        default=(0.0, 0.0),
        default_min=(-250.0, -250.0),
        default_max=(250.0, 250.0),
    ),
    HyprOption(
        key="decoration:shadow:scale",
        section=("decoration", "shadow"),
        name="scale",
        description="shadow's scale. [0.0 - 1.0]",
        type="float",
        default=1.0,
        min=0.0,
        max=1.0,
    ),
    HyprOption(
        key="decoration:dim_modal",
        section=("decoration",),
        name="dim_modal",
        description="enables dimming of parents of modal windows",
        type="bool",
        default=True,
    ),
    HyprOption(
        key="decoration:dim_inactive",
        section=("decoration",),
        name="dim_inactive",
        description="enables dimming of inactive windows",
        type="bool",
        default=False,
    ),
    HyprOption(
        key="decoration:dim_strength",
        section=("decoration",),
        name="dim_strength",
        description="how much inactive windows should be dimmed [0.0 - 1.0]",
        type="float",
        default=0.5,
        min=0.0,
        max=1.0,
    ),
    HyprOption(
        key="decoration:dim_special",
        section=("decoration",),
        name="dim_special",
        description=(
            "how much to dim the rest of the screen by when a special workspace is open. [0.0"
            " - 1.0]"
        ),
        type="float",
        default=0.2,
        min=0.0,
        max=1.0,
    ),
    HyprOption(
        key="decoration:dim_around",
        section=("decoration",),
        name="dim_around",
        description="how much the dimaround window rule should dim by. [0.0 - 1.0]",
        type="float",
        default=0.4,
        min=0.0,
        max=1.0,
    ),
    HyprOption(
        key="decoration:screen_shader",
        section=("decoration",),
        name="screen_shader",
        description=(
            "screen_shader a path to a custom shader to be applied at the end of rendering. S"
            "ee examples/screenShader.frag for an example."
        ),
        type="string",
        default="",
    ),
    HyprOption(
        key="decoration:border_part_of_window",
        section=("decoration",),
        name="border_part_of_window",
        description="whether the border should be treated as a part of the window.",
        type="bool",
        default=True,
    ),
    HyprOption(
        key="decoration:blur:enabled",
        section=("decoration", "blur"),
        name="enabled",
        description="enable kawase window background blur",
        type="bool",
        default=True,
    ),
    HyprOption(
        key="decoration:blur:size",
        section=("decoration", "blur"),
        name="size",
        description="blur size (distance)",
        type="int",
        default=8,
        min=0,
        max=100,
    ),
    HyprOption(
        key="decoration:blur:passes",
        section=("decoration", "blur"),
        name="passes",
        description="the amount of passes to perform",
        type="int",
        default=1,
        min=0,
        max=10,
    ),
    HyprOption(
        key="decoration:blur:ignore_opacity",
        section=("decoration", "blur"),
        name="ignore_opacity",
        description="make the blur layer ignore the opacity of the window",
        type="bool",
        default=True,
    ),
    HyprOption(
        key="decoration:blur:new_optimizations",
        section=("decoration", "blur"),
        name="new_optimizations",
        description=(
            "whether to enable further optimizations to the blur. Recommended to leave on, as"
            " it will massively improve performance."
        ),
        type="bool",
        default=True,
    ),
    HyprOption(
        key="decoration:blur:xray",
        section=("decoration", "blur"),
        name="xray",
        description=(
            "if enabled, floating windows will ignore tiled windows in their blur. Only avail"
            "able if blur_new_optimizations is true. Will reduce overhead on floating blur si"
            "gnificantly."
        ),
        type="bool",
        default=False,
    ),
    HyprOption(
        key="decoration:blur:noise",
        section=("decoration", "blur"),
        name="noise",
        description="how much noise to apply. [0.0 - 1.0]",
        type="float",
        default=0.0117,
        min=0.0,
        max=1.0,
    ),
    HyprOption(
        key="decoration:blur:contrast",
        section=("decoration", "blur"),
        name="contrast",
        description="contrast modulation for blur. [0.0 - 2.0]",
        type="float",
        default=0.8916,
        min=0.0,
        max=2.0,
    ),
    HyprOption(
        key="decoration:blur:brightness",
        section=("decoration", "blur"),
        name="brightness",
        description="brightness modulation for blur. [0.0 - 2.0]",
        type="float",
        default=0.8172,
        min=0.0,
        max=2.0,
    ),
    HyprOption(
        key="decoration:blur:vibrancy",
        section=("decoration", "blur"),
        name="vibrancy",
        description="Increase saturation of blurred colors. [0.0 - 1.0]",
        type="float",
        default=0.1696,
        min=0.0,
        max=1.0,
    ),
    HyprOption(
        key="decoration:blur:vibrancy_darkness",
        section=("decoration", "blur"),
        name="vibrancy_darkness",
        description="How strong the effect of vibrancy is on dark areas . [0.0 - 1.0]",
        type="float",
        default=0.0,
        min=0.0,
        max=1.0,
    ),
    HyprOption(
        key="decoration:blur:special",
        section=("decoration", "blur"),
        name="special",
        description="whether to blur behind the special workspace (note: expensive)",
        type="bool",
        default=False,
    ),
    HyprOption(
        key="decoration:blur:popups",
        section=("decoration", "blur"),
        name="popups",
        description="whether to blur popups (e.g. right-click menus)",
        type="bool",
        default=False,
    ),
    HyprOption(
        key="decoration:blur:popups_ignorealpha",
        section=("decoration", "blur"),
        name="popups_ignorealpha",
        description=(
            "works like ignorealpha in layer rules. If pixel opacity is below set value, will"
            " not blur. [0.0 - 1.0]"
        ),
        type="float",
        default=0.2,
        min=0.0,
        max=1.0,
    ),
    HyprOption(
        key="decoration:blur:input_methods",
        section=("decoration", "blur"),
        name="input_methods",
        description="whether to blur input methods (e.g. fcitx5)",
        type="bool",
        default=False,
    ),
    HyprOption(
        key="decoration:blur:input_methods_ignorealpha",
        section=("decoration", "blur"),
        name="input_methods_ignorealpha",
        description=(
            "works like ignorealpha in layer rules. If pixel opacity is below set value, will"
            " not blur. [0.0 - 1.0]"
        ),
        type="float",
        default=0.2,
        min=0.0,
        max=1.0,
    ),
    HyprOption(
        key="animations:enabled",
        section=("animations",),
        name="enabled",
        description="enable animations",
        type="bool",
        default=True,
    ),
    HyprOption(
        key="animations:workspace_wraparound",
        section=("animations",),
        name="workspace_wraparound",
        description=(
            "changes the direction of slide animations between the first and last workspaces"
        ),
        type="bool",
        default=True,
    ),
    HyprOption(
        key="input:kb_model",
        section=("input",),
        name="kb_model",
        description="Appropriate XKB keymap parameter. See the note below.",
        type="string",
        default="",
    ),
    HyprOption(
        key="input:kb_layout",
        section=("input",),
        name="kb_layout",
        description="Appropriate XKB keymap parameter",
        type="string",
        default="us",
    ),
    HyprOption(
        key="input:kb_variant",
        section=("input",),
        name="kb_variant",
        description="Appropriate XKB keymap parameter",
        type="string",
        default="",
    ),
    HyprOption(
        key="input:kb_options",
        section=("input",),
        name="kb_options",
        description="Appropriate XKB keymap parameter",
        type="string",
        default="",
    ),
    HyprOption(
        key="input:kb_rules",
        section=("input",),
        name="kb_rules",
        description="Appropriate XKB keymap parameter",
        type="string",
        default="",
    ),
    HyprOption(
        key="input:kb_file",
        section=("input",),
        name="kb_file",
        description="Appropriate XKB keymap file",
        type="string",
        default="",
    ),
    HyprOption(
        key="input:numlock_by_default",
        section=("input",),
        name="numlock_by_default",
        description="Engage numlock by default.",
        type="bool",
        default=False,
    ),
    HyprOption(
        key="input:resolve_binds_by_sym",
        section=("input",),
        name="resolve_binds_by_sym",
        description=(
            "Determines how keybinds act when multiple layouts are used. If false, keybinds w"
            "ill always act as if the first specified layout is active. If true, keybinds spe"
            "cified by symbols are activated when you type the respective symbol with the cur"
            "rent layout."
        ),
        type="bool",
        default=False,
    ),
    HyprOption(
        key="input:repeat_rate",
        section=("input",),
        name="repeat_rate",
        description="The repeat rate for held-down keys, in repeats per second.",
        type="int",
        default=25,
        min=0,
        max=200,
    ),
    HyprOption(
        key="input:repeat_delay",
        section=("input",),
        name="repeat_delay",
        description="Delay before a held-down key is repeated, in milliseconds.",
        type="int",
        default=600,
        min=0,
        max=2000,
    ),
    HyprOption(
        key="input:sensitivity",
        section=("input",),
        name="sensitivity",
        description="Sets the mouse input sensitivity. Value is clamped to the range -1.0 to 1.0.",
        type="float",
        default=0.0,
        min=-1.0,
        max=1.0,
    ),
    HyprOption(
        key="input:accel_profile",
        section=("input",),
        name="accel_profile",
        description=(
            "Sets the cursor acceleration profile. Can be one of adaptive, flat. Can also be "
            "custom, see below. Leave empty to use libinput's default mode for your input dev"
            "ice. [adaptive/flat/custom]"
        ),
        type="string",
        default="",
    ),
    HyprOption(
        key="input:force_no_accel",
        section=("input",),
        name="force_no_accel",
        description=(
            "Force no cursor acceleration. This bypasses most of your pointer settings to get"
            " as raw of a signal as possible. Enabling this is not recommended due to potenti"
            "al cursor desynchronization."
        ),
        type="bool",
        default=False,
    ),
    HyprOption(
        key="input:rotation",
        section=("input",),
        name="rotation",
        description=(
            "Sets the rotation of a device in degrees clockwise off the logical neutral posit"
            "ion. Value is clamped to the range 0 to 359."
        ),
        type="int",
        default=0,
        min=0,
        max=359,
    ),
    HyprOption(
        key="input:left_handed",
        section=("input",),
        name="left_handed",
        description="Switches RMB and LMB",
        type="bool",
        default=False,
    ),
    HyprOption(
        key="input:scroll_points",
        section=("input",),
        name="scroll_points",
        description=(
            "Sets the scroll acceleration profile, when accel_profile is set to custom. Has t"
            "o be in the form <step> <points>. Leave empty to have a flat scroll curve."
        ),
        type="string",
        default="",
    ),
    HyprOption(
        key="input:scroll_method",
        section=("input",),
        name="scroll_method",
        description=(
            "Sets the scroll method. Can be one of 2fg (2 fingers), edge, on_button_down, no_"
            "scroll. [2fg/edge/on_button_down/no_scroll]"
        ),
        type="string",
        default="",
    ),
    HyprOption(
        key="input:scroll_button",
        section=("input",),
        name="scroll_button",
        description=(
            "Sets the scroll button. Has to be an int, cannot be a string. Check wev if you h"
            "ave any doubts regarding the ID. 0 means default."
        ),
        type="int",
        default=0,
        min=0,
        max=300,
    ),
    HyprOption(
        key="input:scroll_button_lock",
        section=("input",),
        name="scroll_button_lock",
        description=(
            "If the scroll button lock is enabled, the button does not need to be held down. "
            "Pressing and releasing the button toggles the button lock, which logically holds"
            " the button down or releases it. While the button is logically held down, motion"
            " events are converted to scroll events."
        ),
        type="bool",
        default=False,
    ),
    HyprOption(
        key="input:scroll_factor",
        section=("input",),
        name="scroll_factor",
        description=(
            "Multiplier added to scroll movement for external mice. Note that there is a sepa"
            "rate setting for touchpad scroll_factor."
        ),
        type="float",
        default=1.0,
        min=0.0,
        max=2.0,
    ),
    HyprOption(
        key="input:natural_scroll",
        section=("input",),
        name="natural_scroll",
        description=(
            "Inverts scrolling direction. When enabled, scrolling moves content directly, rat"
            "her than manipulating a scrollbar."
        ),
        type="bool",
        default=False,
    ),
    HyprOption(
        key="input:follow_mouse",
        section=("input",),
        name="follow_mouse",
        description=(
            "Specify if and how cursor movement should affect window focus. See the note belo"
            "w. [0/1/2/3]"
        ),
        type="int",
        default=1,
        min=0,
        max=3,
    ),
    HyprOption(
        key="input:follow_mouse_threshold",
        section=("input",),
        name="follow_mouse_threshold",
        description=(
            "The smallest distance in logical pixels the mouse needs to travel for the window"
            " under it to get focused. Works only with follow_mouse = 1."
        ),
        type="float",
        default=0.0,
        min=0.0,
        max=0.0,
    ),
    HyprOption(
        key="input:focus_on_close",
        section=("input",),
        name="focus_on_close",
        description=(
            "Controls the window focus behavior when a window is closed. When set to 0, focus"
            " will shift to the next window candidate. When set to 1, focus will shift to the"
            " window under the cursor."
        ),
        type="choice",
        default=0,
        enum_values=("next", "cursor"),
        default_str="next",
    ),
    HyprOption(
        key="input:mouse_refocus",
        section=("input",),
        name="mouse_refocus",
        description=(
            "if disabled, mouse focus won't switch to the hovered window unless the mouse cro"
            "sses a window boundary when follow_mouse=1."
        ),
        type="bool",
        default=True,
    ),
    HyprOption(
        key="input:float_switch_override_focus",
        section=("input",),
        name="float_switch_override_focus",
        description=(
            "If enabled (1 or 2), focus will change to the window under the cursor when chang"
            "ing from tiled-to-floating and vice versa. If 2, focus will also follow mouse on"
            " float-to-float switches."
        ),
        type="int",
        default=1,
        min=0,
        max=2,
    ),
    HyprOption(
        key="input:special_fallthrough",
        section=("input",),
        name="special_fallthrough",
        description=(
            "if enabled, having only floating windows in the special workspace will not block"
            " focusing windows in the regular workspace."
        ),
        type="bool",
        default=False,
    ),
    HyprOption(
        key="input:off_window_axis_events",
        section=("input",),
        name="off_window_axis_events",
        description=(
            "Handles axis events around (gaps/border for tiled, dragarea/border for floated) "
            "a focused window. 0 ignores axis events 1 sends out-of-bound coordinates 2 fakes"
            " pointer coordinates to the closest point inside the window 3 warps the cursor t"
            "o the closest point inside the window"
        ),
        type="int",
        default=1,
        min=0,
        max=3,
    ),
    HyprOption(
        key="input:emulate_discrete_scroll",
        section=("input",),
        name="emulate_discrete_scroll",
        description=(
            "Emulates discrete scrolling from high resolution scrolling events. 0 disables it"
            ", 1 enables handling of non-standard events only, and 2 force enables all scroll"
            " wheel events to be handled"
        ),
        type="int",
        default=1,
        min=0,
        max=2,
    ),
    HyprOption(
        key="input:touchpad:disable_while_typing",
        section=("input", "touchpad"),
        name="disable_while_typing",
        description="Disable the touchpad while typing.",
        type="bool",
        default=True,
    ),
    HyprOption(
        key="input:touchpad:natural_scroll",
        section=("input", "touchpad"),
        name="natural_scroll",
        description=(
            "Inverts scrolling direction. When enabled, scrolling moves content directly, rat"
            "her than manipulating a scrollbar."
        ),
        type="bool",
        default=False,
    ),
    HyprOption(
        key="input:touchpad:scroll_factor",
        section=("input", "touchpad"),
        name="scroll_factor",
        description="Multiplier applied to the amount of scroll movement.",
        type="float",
        default=1.0,
        min=0.0,
        max=2.0,
    ),
    HyprOption(
        key="input:touchpad:middle_button_emulation",
        section=("input", "touchpad"),
        name="middle_button_emulation",
        description=(
            "Sending LMB and RMB simultaneously will be interpreted as a middle click. This d"
            "isables any touchpad area that would normally send a middle click based on locat"
            "ion."
        ),
        type="bool",
        default=False,
    ),
    HyprOption(
        key="input:touchpad:tap_button_map",
        section=("input", "touchpad"),
        name="tap_button_map",
        description=(
            "Sets the tap button mapping for touchpad button emulation. Can be one of lrm (de"
            "fault) or lmr (Left, Middle, Right Buttons). [lrm/lmr]"
        ),
        type="string",
        default="",
    ),
    HyprOption(
        key="input:touchpad:clickfinger_behavior",
        section=("input", "touchpad"),
        name="clickfinger_behavior",
        description=(
            "Button presses with 1, 2, or 3 fingers will be mapped to LMB, RMB, and MMB respe"
            "ctively. This disables interpretation of clicks based on location on the touchpa"
            "d."
        ),
        type="bool",
        default=False,
    ),
    HyprOption(
        key="input:touchpad:tap-to-click",
        section=("input", "touchpad"),
        name="tap-to-click",
        description=(
            "Tapping on the touchpad with 1, 2, or 3 fingers will send LMB, RMB, and MMB resp"
            "ectively."
        ),
        type="bool",
        default=True,
    ),
    HyprOption(
        key="input:touchpad:drag_lock",
        section=("input", "touchpad"),
        name="drag_lock",
        description=(
            "When enabled, lifting the finger off while dragging will not drop the dragged it"
            "em. 0 -> disabled, 1 -> enabled with timeout, 2 -> enabled sticky.dragging will "
            "not drop the dragged item."
        ),
        type="int",
        default=0,
        min=0,
        max=2,
    ),
    HyprOption(
        key="input:touchpad:tap-and-drag",
        section=("input", "touchpad"),
        name="tap-and-drag",
        description="Sets the tap and drag mode for the touchpad",
        type="bool",
        default=False,
    ),
    HyprOption(
        key="input:touchpad:flip_x",
        section=("input", "touchpad"),
        name="flip_x",
        description="Inverts the horizontal movement of the touchpad",
        type="bool",
        default=False,
    ),
    HyprOption(
        key="input:touchpad:flip_y",
        section=("input", "touchpad"),
        name="flip_y",
        description="Inverts the vertical movement of the touchpad",
        type="bool",
        default=False,
    ),
    HyprOption(
        key="input:touchpad:drag_3fg",
        section=("input", "touchpad"),
        name="drag_3fg",
        description="Three Finger Drag 0 -> disabled, 1 -> 3 finger, 2 -> 4 finger",
        type="int",
        default=0,
        min=0,
        max=2,
    ),
    HyprOption(
        key="input:touchdevice:transform",
        section=("input", "touchdevice"),
        name="transform",
        description=(
            "Transform the input from touchdevices. The possible transformations are the same"
            " as those of the monitors"
        ),
        type="int",
        default=0,
        min=0,
        max=6,
    ),
    HyprOption(
        key="input:touchdevice:output",
        section=("input", "touchdevice"),
        name="output",
        description=(
            "The monitor to bind touch devices. The default is auto-detection. To stop auto-d"
            "etection, use an empty string or the [[Empty]] value."
        ),
        type="string",
        default="",
    ),
    HyprOption(
        key="input:touchdevice:enabled",
        section=("input", "touchdevice"),
        name="enabled",
        description="Whether input is enabled for touch devices.",
        type="bool",
        default=True,
    ),
    HyprOption(
        key="input:virtualkeyboard:share_states",
        section=("input", "virtualkeyboard"),
        name="share_states",
        description=(
            "Unify key down states and modifier states with other keyboards. 0 -> no, 1 -> ye"
            "s, 2 -> yes unless IME client"
        ),
        type="int",
        default=2,
        min=0,
        max=2,
    ),
    HyprOption(
        key="input:virtualkeyboard:release_pressed_on_close",
        section=("input", "virtualkeyboard"),
        name="release_pressed_on_close",
        description="Release all pressed keys by virtual keyboard on close.",
        type="bool",
        default=False,
    ),
    HyprOption(
        key="input:tablet:transform",
        section=("input", "tablet"),
        name="transform",
        description=(
            "transform the input from tablets. The possible transformations are the same as t"
            "hose of the monitors"
        ),
        type="int",
        default=0,
        min=0,
        max=6,
    ),
    HyprOption(
        key="input:tablet:output",
        section=("input", "tablet"),
        name="output",
        description=(
            "the monitor to bind tablets. Can be current or a monitor name. Leave empty to ma"
            "p across all monitors."
        ),
        type="string",
        default="",
    ),
    HyprOption(
        key="input:tablet:region_position",
        section=("input", "tablet"),
        name="region_position",
        description=(
            "position of the mapped region in monitor layout relative to the top left corner "
            "of the bound monitor or all monitors."
        ),
        type="vec2",
        default=(0.0, 0.0),
        default_min=(-20000.0, -20000.0),
        default_max=(20000.0, 20000.0),
    ),
    HyprOption(
        key="input:tablet:absolute_region_position",
        section=("input", "tablet"),
        name="absolute_region_position",
        description=(
            "whether to treat the region_position as an absolute position in monitor layout. "
            "Only applies when output is empty."
        ),
        type="bool",
        default=False,
    ),
    HyprOption(
        key="input:tablet:region_size",
        section=("input", "tablet"),
        name="region_size",
        description=(
            "size of the mapped region. When this variable is set, tablet input will be mappe"
            "d to the region. [0, 0] or invalid size means unset."
        ),
        type="vec2",
        default=(0.0, 0.0),
        default_min=(-100.0, -100.0),
        default_max=(4000.0, 4000.0),
    ),
    HyprOption(
        key="input:tablet:relative_input",
        section=("input", "tablet"),
        name="relative_input",
        description="whether the input should be relative",
        type="bool",
        default=False,
    ),
    HyprOption(
        key="input:tablet:left_handed",
        section=("input", "tablet"),
        name="left_handed",
        description="if enabled, the tablet will be rotated 180 degrees",
        type="bool",
        default=False,
    ),
    HyprOption(
        key="input:tablet:active_area_size",
        section=("input", "tablet"),
        name="active_area_size",
        description="size of tablet's active area in mm",
        type="vec2",
        default=(0.0, 0.0),
        default_min=(0.0, 0.0),
        default_max=(500.0, 500.0),
    ),
    HyprOption(
        key="input:tablet:active_area_position",
        section=("input", "tablet"),
        name="active_area_position",
        description="position of the active area in mm",
        type="vec2",
        default=(0.0, 0.0),
        default_min=(0.0, 0.0),
        default_max=(500.0, 500.0),
    ),
    HyprOption(
        key="gestures:workspace_swipe_distance",
        section=("gestures",),
        name="workspace_swipe_distance",
        description="in px, the distance of the touchpad gesture",
        type="int",
        default=300,
        min=0,
        max=2000,
    ),
    HyprOption(
        key="gestures:workspace_swipe_touch",
        section=("gestures",),
        name="workspace_swipe_touch",
        description="enable workspace swiping from the edge of a touchscreen",
        type="bool",
        default=False,
    ),
    HyprOption(
        key="gestures:workspace_swipe_invert",
        section=("gestures",),
        name="workspace_swipe_invert",
        description="invert the direction (touchpad only)",
        type="bool",
        default=True,
    ),
    HyprOption(
        key="gestures:workspace_swipe_touch_invert",
        section=("gestures",),
        name="workspace_swipe_touch_invert",
        description="invert the direction (touchscreen only)",
        type="bool",
        default=False,
    ),
    HyprOption(
        key="gestures:workspace_swipe_min_speed_to_force",
        section=("gestures",),
        name="workspace_swipe_min_speed_to_force",
        description=(
            "minimum speed in px per timepoint to force the change ignoring cancel_ratio. Set"
            "ting to 0 will disable this mechanic."
        ),
        type="int",
        default=30,
        min=0,
        max=200,
    ),
    HyprOption(
        key="gestures:workspace_swipe_cancel_ratio",
        section=("gestures",),
        name="workspace_swipe_cancel_ratio",
        description=(
            "how much the swipe has to proceed in order to commence it. (0.7 -> if > 0.7 * di"
            "stance, switch, if less, revert) [0.0 - 1.0]"
        ),
        type="float",
        default=0.5,
        min=0.0,
        max=1.0,
    ),
    HyprOption(
        key="gestures:workspace_swipe_create_new",
        section=("gestures",),
        name="workspace_swipe_create_new",
        description="whether a swipe right on the last workspace should create a new one.",
        type="bool",
        default=True,
    ),
    HyprOption(
        key="gestures:workspace_swipe_direction_lock",
        section=("gestures",),
        name="workspace_swipe_direction_lock",
        description=(
            "if enabled, switching direction will be locked when you swipe past the direction"
            "_lock_threshold (touchpad only)."
        ),
        type="bool",
        default=True,
    ),
    HyprOption(
        key="gestures:workspace_swipe_direction_lock_threshold",
        section=("gestures",),
        name="workspace_swipe_direction_lock_threshold",
        description="in px, the distance to swipe before direction lock activates (touchpad only).",
        type="int",
        default=10,
        min=0,
        max=200,
    ),
    HyprOption(
        key="gestures:workspace_swipe_forever",
        section=("gestures",),
        name="workspace_swipe_forever",
        description=(
            "if enabled, swiping will not clamp at the neighboring workspaces but continue to"
            " the further ones."
        ),
        type="bool",
        default=False,
    ),
    HyprOption(
        key="gestures:workspace_swipe_use_r",
        section=("gestures",),
        name="workspace_swipe_use_r",
        description=(
            "if enabled, swiping will use the r prefix instead of the m prefix for finding wo"
            "rkspaces."
        ),
        type="bool",
        default=False,
    ),
    HyprOption(
        key="gestures:close_max_timeout",
        section=("gestures",),
        name="close_max_timeout",
        description="Timeout for closing windows with the close gesture, in ms.",
        type="int",
        default=1000,
        min=10,
        max=2000,
    ),
    HyprOption(
        key="group:insert_after_current",
        section=("group",),
        name="insert_after_current",
        description="whether new windows in a group spawn after current or at group tail",
        type="bool",
        default=True,
    ),
    HyprOption(
        key="group:focus_removed_window",
        section=("group",),
        name="focus_removed_window",
        description=(
            "whether Hyprland should focus on the window that has just been moved out of the group"
        ),
        type="bool",
        default=True,
    ),
    HyprOption(
        key="group:merge_groups_on_drag",
        section=("group",),
        name="merge_groups_on_drag",
        description="whether window groups can be dragged into other groups",
        type="bool",
        default=True,
    ),
    HyprOption(
        key="group:merge_groups_on_groupbar",
        section=("group",),
        name="merge_groups_on_groupbar",
        description="whether one group will be merged with another when dragged into its groupbar",
        type="bool",
        default=True,
    ),
    HyprOption(
        key="group:col.border_active",
        section=("group",),
        name="col.border_active",
        description="border color for inactive windows",
        type="gradient",
        default="0x66ffff00",
    ),
    HyprOption(
        key="group:col.border_inactive",
        section=("group",),
        name="col.border_inactive",
        description="border color for the active window",
        type="gradient",
        default="0x66777700",
    ),
    HyprOption(
        key="group:col.border_locked_inactive",
        section=("group",),
        name="col.border_locked_inactive",
        description=(
            "inactive border color for window that cannot be added to a group (see denywindow"
            "fromgroup dispatcher)"
        ),
        type="gradient",
        default="0x66ff5500",
    ),
    HyprOption(
        key="group:col.border_locked_active",
        section=("group",),
        name="col.border_locked_active",
        description="active border color for window that cannot be added to a group",
        type="gradient",
        default="0x66775500",
    ),
    HyprOption(
        key="group:auto_group",
        section=("group",),
        name="auto_group",
        description="automatically group new windows",
        type="bool",
        default=True,
    ),
    HyprOption(
        key="group:drag_into_group",
        section=("group",),
        name="drag_into_group",
        description=(
            "whether dragging a window into a unlocked group will merge them. Options: 0 (dis"
            "abled), 1 (enabled), 2 (only when dragging into the groupbar)"
        ),
        type="choice",
        default=0,
        enum_values=("disabled", "enabled", "only when dragging into the groupbar"),
        default_str="disabled",
    ),
    HyprOption(
        key="group:merge_floated_into_tiled_on_groupbar",
        section=("group",),
        name="merge_floated_into_tiled_on_groupbar",
        description=(
            "whether dragging a floating window into a tiled window groupbar will merge them"
        ),
        type="bool",
        default=False,
    ),
    HyprOption(
        key="group:group_on_movetoworkspace",
        section=("group",),
        name="group_on_movetoworkspace",
        description=(
            "whether using movetoworkspace[silent] will merge the window into the workspace's"
            " solitary unlocked group"
        ),
        type="bool",
        default=False,
    ),
    HyprOption(
        key="group:groupbar:enabled",
        section=("group", "groupbar"),
        name="enabled",
        description="enables groupbars",
        type="bool",
        default=True,
    ),
    HyprOption(
        key="group:groupbar:font_family",
        section=("group", "groupbar"),
        name="font_family",
        description="font used to display groupbar titles, use misc:font_family if not specified",
        type="string",
        default="",
    ),
    HyprOption(
        key="group:groupbar:font_weight_active",
        section=("group", "groupbar"),
        name="font_weight_active",
        description="weight of the font used to display active groupbar titles",
        type="string",
        default="normal",
    ),
    HyprOption(
        key="group:groupbar:font_weight_inactive",
        section=("group", "groupbar"),
        name="font_weight_inactive",
        description="weight of the font used to display inactive groupbar titles",
        type="string",
        default="normal",
    ),
    HyprOption(
        key="group:groupbar:font_size",
        section=("group", "groupbar"),
        name="font_size",
        description="font size of groupbar title",
        type="int",
        default=8,
        min=2,
        max=64,
    ),
    HyprOption(
        key="group:groupbar:gradients",
        section=("group", "groupbar"),
        name="gradients",
        description="enables gradients",
        type="bool",
        default=False,
    ),
    HyprOption(
        key="group:groupbar:height",
        section=("group", "groupbar"),
        name="height",
        description="height of the groupbar",
        type="int",
        default=14,
        min=1,
        max=64,
    ),
    HyprOption(
        key="group:groupbar:indicator_gap",
        section=("group", "groupbar"),
        name="indicator_gap",
        description="height of the gap between the groupbar indicator and title",
        type="int",
        default=0,
        min=0,
        max=64,
    ),
    HyprOption(
        key="group:groupbar:indicator_height",
        section=("group", "groupbar"),
        name="indicator_height",
        description="height of the groupbar indicator",
        type="int",
        default=3,
        min=1,
        max=64,
    ),
    HyprOption(
        key="group:groupbar:stacked",
        section=("group", "groupbar"),
        name="stacked",
        description="render the groupbar as a vertical stack",
        type="bool",
        default=False,
    ),
    HyprOption(
        key="group:groupbar:priority",
        section=("group", "groupbar"),
        name="priority",
        description="sets the decoration priority for groupbars",
        type="int",
        default=3,
        min=0,
        max=6,
    ),
    HyprOption(
        key="group:groupbar:render_titles",
        section=("group", "groupbar"),
        name="render_titles",
        description="whether to render titles in the group bar decoration",
        type="bool",
        default=True,
    ),
    HyprOption(
        key="group:groupbar:scrolling",
        section=("group", "groupbar"),
        name="scrolling",
        description="whether scrolling in the groupbar changes group active window",
        type="bool",
        default=True,
    ),
    HyprOption(
        key="group:groupbar:rounding",
        section=("group", "groupbar"),
        name="rounding",
        description="how much to round the groupbar",
        type="int",
        default=1,
        min=0,
        max=20,
    ),
    HyprOption(
        key="group:groupbar:rounding_power",
        section=("group", "groupbar"),
        name="rounding_power",
        description="rounding power of groupbar corners (2 is a circle)",
        type="float",
        default=2.0,
        min=2.0,
        max=10.0,
    ),
    HyprOption(
        key="group:groupbar:gradient_rounding",
        section=("group", "groupbar"),
        name="gradient_rounding",
        description="how much to round the groupbar gradient",
        type="int",
        default=1,
        min=0,
        max=20,
    ),
    HyprOption(
        key="group:groupbar:gradient_rounding_power",
        section=("group", "groupbar"),
        name="gradient_rounding_power",
        description="rounding power of groupbar gradient corners (2 is a circle)",
        type="float",
        default=2.0,
        min=2.0,
        max=10.0,
    ),
    HyprOption(
        key="group:groupbar:round_only_edges",
        section=("group", "groupbar"),
        name="round_only_edges",
        description="if yes, will only round at the groupbar edges",
        type="bool",
        default=True,
    ),
    HyprOption(
        key="group:groupbar:gradient_round_only_edges",
        section=("group", "groupbar"),
        name="gradient_round_only_edges",
        description="if yes, will only round at the groupbar gradient edges",
        type="bool",
        default=True,
    ),
    HyprOption(
        key="group:groupbar:text_color",
        section=("group", "groupbar"),
        name="text_color",
        description="color for window titles in the groupbar",
        type="color",
        default="0xffffffff",
    ),
    HyprOption(
        key="group:groupbar:text_color_inactive",
        section=("group", "groupbar"),
        name="text_color_inactive",
        description=(
            "color for inactive windows' titles in the groupbar (if unset, defaults to text_color)"
        ),
        type="color",
        default=None,
    ),
    HyprOption(
        key="group:groupbar:text_color_locked_active",
        section=("group", "groupbar"),
        name="text_color_locked_active",
        description=(
            "color for the active window's title in a locked group (if unset, defaults to tex"
            "t_color)"
        ),
        type="color",
        default=None,
    ),
    HyprOption(
        key="group:groupbar:text_color_locked_inactive",
        section=("group", "groupbar"),
        name="text_color_locked_inactive",
        description=(
            "color for inactive windows' titles in locked groups (if unset, defaults to text_"
            "color_inactive)"
        ),
        type="color",
        default=None,
    ),
    HyprOption(
        key="group:groupbar:col.active",
        section=("group", "groupbar"),
        name="col.active",
        description="active group border color",
        type="color",
        default="0x66ffff00",
    ),
    HyprOption(
        key="group:groupbar:col.inactive",
        section=("group", "groupbar"),
        name="col.inactive",
        description="inactive (out of focus) group border color",
        type="color",
        default="0x66777700",
    ),
    HyprOption(
        key="group:groupbar:col.locked_active",
        section=("group", "groupbar"),
        name="col.locked_active",
        description="active locked group border color",
        type="color",
        default="0x66ff5500",
    ),
    HyprOption(
        key="group:groupbar:col.locked_inactive",
        section=("group", "groupbar"),
        name="col.locked_inactive",
        description="controls the group bar text color",
        type="color",
        default="0x66775500",
    ),
    HyprOption(
        key="group:groupbar:gaps_out",
        section=("group", "groupbar"),
        name="gaps_out",
        description="gap between gradients and window",
        type="int",
        default=2,
        min=0,
        max=20,
    ),
    HyprOption(
        key="group:groupbar:gaps_in",
        section=("group", "groupbar"),
        name="gaps_in",
        description="gap between gradients",
        type="int",
        default=2,
        min=0,
        max=20,
    ),
    HyprOption(
        key="group:groupbar:keep_upper_gap",
        section=("group", "groupbar"),
        name="keep_upper_gap",
        description="keep an upper gap above gradient",
        type="bool",
        default=True,
    ),
    HyprOption(
        key="group:groupbar:text_offset",
        section=("group", "groupbar"),
        name="text_offset",
        description="set an offset for a text",
        type="int",
        default=0,
        min=-20,
        max=20,
    ),
    HyprOption(
        key="group:groupbar:text_padding",
        section=("group", "groupbar"),
        name="text_padding",
        description="set horizontal padding for a text",
        type="int",
        default=0,
        min=0,
        max=22,
    ),
    HyprOption(
        key="group:groupbar:blur",
        section=("group", "groupbar"),
        name="blur",
        description="enable background blur for groupbars",
        type="bool",
        default=False,
    ),
    HyprOption(
        key="misc:disable_hyprland_logo",
        section=("misc",),
        name="disable_hyprland_logo",
        description="disables the random Hyprland logo / anime girl background. :(",
        type="bool",
        default=False,
    ),
    HyprOption(
        key="misc:disable_splash_rendering",
        section=("misc",),
        name="disable_splash_rendering",
        description=(
            "disables the Hyprland splash rendering. (requires a monitor reload to take effect)"
        ),
        type="bool",
        default=False,
    ),
    HyprOption(
        key="misc:col.splash",
        section=("misc",),
        name="col.splash",
        description=(
            "Changes the color of the splash text (requires a monitor reload to take effect)."
        ),
        type="color",
        default="0xffffffff",
    ),
    HyprOption(
        key="misc:font_family",
        section=("misc",),
        name="font_family",
        description=(
            "Set the global default font to render the text including debug fps/notification,"
            " config error messages and etc., selected from system fonts."
        ),
        type="string",
        default="Sans",
    ),
    HyprOption(
        key="misc:splash_font_family",
        section=("misc",),
        name="splash_font_family",
        description=(
            "Changes the font used to render the splash text, selected from system fonts (req"
            "uires a monitor reload to take effect)."
        ),
        type="string",
        default="",
    ),
    HyprOption(
        key="misc:force_default_wallpaper",
        section=("misc",),
        name="force_default_wallpaper",
        description=(
            "Enforce any of the 3 default wallpapers. Setting this to 0 or 1 disables the ani"
            "me background. -1 means “random”. [-1/0/1/2]"
        ),
        type="int",
        default=-1,
        min=-1,
        max=2,
    ),
    HyprOption(
        key="misc:vfr",
        section=("misc",),
        name="vfr",
        description=(
            "controls the VFR status of Hyprland. Heavily recommended to leave enabled to con"
            "serve resources."
        ),
        type="bool",
        default=True,
    ),
    HyprOption(
        key="misc:vrr",
        section=("misc",),
        name="vrr",
        description=(
            "controls the VRR (Adaptive Sync) of your monitors. 0 - off, 1 - on, 2 - fullscre"
            "en only, 3 - fullscreen with game or video content type [0/1/2/3]"
        ),
        type="int",
        default=0,
        min=0,
        max=3,
    ),
    HyprOption(
        key="misc:mouse_move_enables_dpms",
        section=("misc",),
        name="mouse_move_enables_dpms",
        description="If DPMS is set to off, wake up the monitors if the mouse move",
        type="bool",
        default=False,
    ),
    HyprOption(
        key="misc:key_press_enables_dpms",
        section=("misc",),
        name="key_press_enables_dpms",
        description="If DPMS is set to off, wake up the monitors if a key is pressed.",
        type="bool",
        default=False,
    ),
    HyprOption(
        key="misc:name_vk_after_proc",
        section=("misc",),
        name="name_vk_after_proc",
        description=(
            "Name virtual keyboards after the processes that create them. E.g. /usr/bin/fcitx"
            "5 will have hl-virtual-keyboard-fcitx5."
        ),
        type="bool",
        default=True,
    ),
    HyprOption(
        key="misc:always_follow_on_dnd",
        section=("misc",),
        name="always_follow_on_dnd",
        description=(
            "Will make mouse focus follow the mouse when drag and dropping. Recommended to le"
            "ave it enabled, especially for people using focus follows mouse at 0."
        ),
        type="bool",
        default=True,
    ),
    HyprOption(
        key="misc:layers_hog_keyboard_focus",
        section=("misc",),
        name="layers_hog_keyboard_focus",
        description=(
            "If true, will make keyboard-interactive layers keep their focus on mouse move (e"
            ".g. wofi, bemenu)"
        ),
        type="bool",
        default=True,
    ),
    HyprOption(
        key="misc:animate_manual_resizes",
        section=("misc",),
        name="animate_manual_resizes",
        description="If true, will animate manual window resizes/moves",
        type="bool",
        default=False,
    ),
    HyprOption(
        key="misc:animate_mouse_windowdragging",
        section=("misc",),
        name="animate_mouse_windowdragging",
        description=(
            "If true, will animate windows being dragged by mouse, note that this can cause w"
            "eird behavior on some curves"
        ),
        type="bool",
        default=False,
    ),
    HyprOption(
        key="misc:disable_autoreload",
        section=("misc",),
        name="disable_autoreload",
        description=(
            "If true, the config will not reload automatically on save, and instead needs to "
            "be reloaded with hyprctl reload. Might save on battery."
        ),
        type="bool",
        default=False,
    ),
    HyprOption(
        key="misc:enable_swallow",
        section=("misc",),
        name="enable_swallow",
        description="Enable window swallowing",
        type="bool",
        default=False,
    ),
    HyprOption(
        key="misc:swallow_regex",
        section=("misc",),
        name="swallow_regex",
        description=(
            "The class regex to be used for windows that should be swallowed (usually, a term"
            "inal). To know more about the list of regex which can be used use this cheatshee"
            "t."
        ),
        type="string",
        default="",
    ),
    HyprOption(
        key="misc:swallow_exception_regex",
        section=("misc",),
        name="swallow_exception_regex",
        description=(
            "The title regex to be used for windows that should not be swallowed by the windo"
            "ws specified in swallow_regex (e.g. wev). The regex is matched against the paren"
            "t (e.g. Kitty) window’s title on the assumption that it changes to whatever proc"
            "ess it’s running."
        ),
        type="string",
        default="",
    ),
    HyprOption(
        key="misc:focus_on_activate",
        section=("misc",),
        name="focus_on_activate",
        description=(
            "Whether Hyprland should focus an app that requests to be focused (an activate request)"
        ),
        type="bool",
        default=False,
    ),
    HyprOption(
        key="misc:mouse_move_focuses_monitor",
        section=("misc",),
        name="mouse_move_focuses_monitor",
        description="Whether mouse moving into a different monitor should focus it",
        type="bool",
        default=True,
    ),
    HyprOption(
        key="misc:allow_session_lock_restore",
        section=("misc",),
        name="allow_session_lock_restore",
        description=(
            "if true, will allow you to restart a lockscreen app in case it crashes (red scre"
            "en of death)"
        ),
        type="bool",
        default=False,
    ),
    HyprOption(
        key="misc:session_lock_xray",
        section=("misc",),
        name="session_lock_xray",
        description="keep rendering workspaces below your lockscreen",
        type="bool",
        default=False,
    ),
    HyprOption(
        key="misc:background_color",
        section=("misc",),
        name="background_color",
        description="change the background color. (requires enabled disable_hyprland_logo)",
        type="color",
        default="0x00111111",
    ),
    HyprOption(
        key="misc:close_special_on_empty",
        section=("misc",),
        name="close_special_on_empty",
        description="close the special workspace if the last window is removed",
        type="bool",
        default=True,
    ),
    HyprOption(
        key="misc:on_focus_under_fullscreen",
        section=("misc",),
        name="on_focus_under_fullscreen",
        description=(
            "if there is a fullscreen or maximized window, decide whether a tiled window requ"
            "ested to focus should replace it, stay behind or disable the fullscreen/maximize"
            "d state. 0 - ignore focus request (keep focus on fullscreen window), 1 - takes o"
            "ver, 2 - unfullscreen/unmaximize [0/1/2]"
        ),
        type="int",
        default=2,
        min=0,
        max=2,
    ),
    HyprOption(
        key="misc:exit_window_retains_fullscreen",
        section=("misc",),
        name="exit_window_retains_fullscreen",
        description="if true, closing a fullscreen window makes the next focused window fullscreen",
        type="bool",
        default=False,
    ),
    HyprOption(
        key="misc:initial_workspace_tracking",
        section=("misc",),
        name="initial_workspace_tracking",
        description=(
            "if enabled, windows will open on the workspace they were invoked on. 0 - disable"
            "d, 1 - single-shot, 2 - persistent (all children too)"
        ),
        type="int",
        default=1,
        min=0,
        max=2,
    ),
    HyprOption(
        key="misc:middle_click_paste",
        section=("misc",),
        name="middle_click_paste",
        description="whether to enable middle-click-paste (aka primary selection)",
        type="bool",
        default=True,
    ),
    HyprOption(
        key="misc:render_unfocused_fps",
        section=("misc",),
        name="render_unfocused_fps",
        description="the maximum limit for renderunfocused windows' fps in the background",
        type="int",
        default=15,
        min=1,
        max=120,
    ),
    HyprOption(
        key="misc:disable_xdg_env_checks",
        section=("misc",),
        name="disable_xdg_env_checks",
        description="disable the warning if XDG environment is externally managed",
        type="bool",
        default=False,
    ),
    HyprOption(
        key="misc:disable_hyprland_guiutils_check",
        section=("misc",),
        name="disable_hyprland_guiutils_check",
        description="disable the warning if hyprland-guiutils is missing",
        type="bool",
        default=False,
    ),
    HyprOption(
        key="misc:disable_watchdog_warning",
        section=("misc",),
        name="disable_watchdog_warning",
        description="whether to disable the warning about not using start-hyprland.",
        type="bool",
        default=False,
    ),
    HyprOption(
        key="misc:lockdead_screen_delay",
        section=("misc",),
        name="lockdead_screen_delay",
        description=(
            "the delay in ms after the lockdead screen appears if the lock screen did not app"
            "ear after a lock event occurred."
        ),
        type="int",
        default=1000,
        min=0,
        max=5000,
    ),
    HyprOption(
        key="misc:enable_anr_dialog",
        section=("misc",),
        name="enable_anr_dialog",
        description="whether to enable the ANR (app not responding) dialog when your apps hang",
        type="bool",
        default=True,
    ),
    HyprOption(
        key="misc:anr_missed_pings",
        section=("misc",),
        name="anr_missed_pings",
        description="number of missed pings before showing the ANR dialog",
        type="int",
        default=5,
        min=1,
        max=20,
    ),
    HyprOption(
        key="misc:screencopy_force_8b",
        section=("misc",),
        name="screencopy_force_8b",
        description="forces 8 bit screencopy (fixes apps that don't understand 10bit)",
        type="bool",
        default=True,
    ),
    HyprOption(
        key="misc:disable_scale_notification",
        section=("misc",),
        name="disable_scale_notification",
        description=(
            "disables notification popup when a monitor fails to set a suitable scale and fal"
            "ls back to suggested"
        ),
        type="bool",
        default=False,
    ),
    HyprOption(
        key="misc:size_limits_tiled",
        section=("misc",),
        name="size_limits_tiled",
        description="whether to apply minsize and maxsize rules to tiled windows",
        type="bool",
        default=False,
    ),
    HyprOption(
        key="binds:pass_mouse_when_bound",
        section=("binds",),
        name="pass_mouse_when_bound",
        description=(
            "if disabled, will not pass the mouse events to apps / dragging windows around if"
            " a keybind has been triggered."
        ),
        type="bool",
        default=False,
    ),
    HyprOption(
        key="binds:scroll_event_delay",
        section=("binds",),
        name="scroll_event_delay",
        description=(
            "in ms, how many ms to wait after a scroll event to allow passing another one for"
            " the binds."
        ),
        type="int",
        default=300,
        min=0,
        max=2000,
    ),
    HyprOption(
        key="binds:workspace_back_and_forth",
        section=("binds",),
        name="workspace_back_and_forth",
        description=(
            "If enabled, an attempt to switch to the currently focused workspace will instead"
            " switch to the previous workspace. Akin to i3’s auto_back_and_forth."
        ),
        type="bool",
        default=False,
    ),
    HyprOption(
        key="binds:hide_special_on_workspace_change",
        section=("binds",),
        name="hide_special_on_workspace_change",
        description=(
            "If enabled, changing the active workspace (including to itself) will hide the sp"
            "ecial workspace on the monitor where the newly active workspace resides."
        ),
        type="bool",
        default=False,
    ),
    HyprOption(
        key="binds:allow_workspace_cycles",
        section=("binds",),
        name="allow_workspace_cycles",
        description=(
            "If enabled, workspaces don’t forget their previous workspace, so cycles can be c"
            "reated by switching to the first workspace in a sequence, then endlessly going t"
            "o the previous workspace."
        ),
        type="bool",
        default=False,
    ),
    HyprOption(
        key="binds:workspace_center_on",
        section=("binds",),
        name="workspace_center_on",
        description=(
            "Whether switching workspaces should center the cursor on the workspace (0) or on"
            " the last active window for that workspace (1)"
        ),
        type="int",
        default=0,
        min=0,
        max=1,
    ),
    HyprOption(
        key="binds:focus_preferred_method",
        section=("binds",),
        name="focus_preferred_method",
        description=(
            "sets the preferred focus finding method when using focuswindow/movewindow/etc wi"
            "th a direction. 0 - history (recent have priority), 1 - length (longer shared ed"
            "ges have priority)"
        ),
        type="int",
        default=0,
        min=0,
        max=1,
    ),
    HyprOption(
        key="binds:ignore_group_lock",
        section=("binds",),
        name="ignore_group_lock",
        description=(
            "If enabled, dispatchers like moveintogroup, moveoutofgroup and movewindoworgroup"
            " will ignore lock per group."
        ),
        type="bool",
        default=False,
    ),
    HyprOption(
        key="binds:movefocus_cycles_fullscreen",
        section=("binds",),
        name="movefocus_cycles_fullscreen",
        description=(
            "If enabled, when on a fullscreen window, movefocus will cycle fullscreen, if not"
            ", it will move the focus in a direction."
        ),
        type="bool",
        default=False,
    ),
    HyprOption(
        key="binds:movefocus_cycles_groupfirst",
        section=("binds",),
        name="movefocus_cycles_groupfirst",
        description=(
            "If enabled, when in a grouped window, movefocus will cycle windows in the groups"
            " first, then at each ends of tabs, it'll move on to other windows/groups"
        ),
        type="bool",
        default=False,
    ),
    HyprOption(
        key="binds:disable_keybind_grabbing",
        section=("binds",),
        name="disable_keybind_grabbing",
        description=(
            "If enabled, apps that request keybinds to be disabled (e.g. VMs) will not be abl"
            "e to do so."
        ),
        type="bool",
        default=False,
    ),
    HyprOption(
        key="binds:window_direction_monitor_fallback",
        section=("binds",),
        name="window_direction_monitor_fallback",
        description=(
            "If enabled, moving a window or focus over the edge of a monitor with a direction"
            " will move it to the next monitor in that direction."
        ),
        type="bool",
        default=True,
    ),
    HyprOption(
        key="binds:allow_pin_fullscreen",
        section=("binds",),
        name="allow_pin_fullscreen",
        description=(
            "Allows fullscreen to pinned windows, and restore their pinned status afterwards"
        ),
        type="bool",
        default=False,
    ),
    HyprOption(
        key="binds:drag_threshold",
        section=("binds",),
        name="drag_threshold",
        description=(
            "Movement threshold in pixels for window dragging and c/g bind flags. 0 to disabl"
            "e and grab on mousedown."
        ),
        type="int",
        default=0,
        min=0,
        max=2147483647,
    ),
    HyprOption(
        key="xwayland:enabled",
        section=("xwayland",),
        name="enabled",
        description="allow running applications using X11",
        type="bool",
        default=True,
    ),
    HyprOption(
        key="xwayland:use_nearest_neighbor",
        section=("xwayland",),
        name="use_nearest_neighbor",
        description=(
            "uses the nearest neighbor filtering for xwayland apps, making them pixelated rat"
            "her than blurry"
        ),
        type="bool",
        default=True,
    ),
    HyprOption(
        key="xwayland:force_zero_scaling",
        section=("xwayland",),
        name="force_zero_scaling",
        description="forces a scale of 1 on xwayland windows on scaled displays.",
        type="bool",
        default=False,
    ),
    HyprOption(
        key="xwayland:create_abstract_socket",
        section=("xwayland",),
        name="create_abstract_socket",
        description="Create the abstract Unix domain socket for XWayland",
        type="bool",
        default=False,
    ),
    HyprOption(
        key="opengl:nvidia_anti_flicker",
        section=("opengl",),
        name="nvidia_anti_flicker",
        description=(
            "reduces flickering on nvidia at the cost of possible frame drops on lower-end GP"
            "Us. On non-nvidia, this is ignored."
        ),
        type="bool",
        default=True,
    ),
    HyprOption(
        key="render:direct_scanout",
        section=("render",),
        name="direct_scanout",
        description=(
            "Enables direct scanout. Direct scanout attempts to reduce lag when there is only"
            " one fullscreen application on a screen (e.g. game). It is also recommended to s"
            "et this to false if the fullscreen application shows graphical glitches. 0 - off"
            ", 1 - on, 2 - auto (on with content type 'game')"
        ),
        type="int",
        default=0,
        min=0,
        max=2,
    ),
    HyprOption(
        key="render:expand_undersized_textures",
        section=("render",),
        name="expand_undersized_textures",
        description=(
            "Whether to expand textures that have not yet resized to be larger, or to just st"
            "retch them instead."
        ),
        type="bool",
        default=True,
    ),
    HyprOption(
        key="render:xp_mode",
        section=("render",),
        name="xp_mode",
        description="Disable back buffer and bottom layer rendering.",
        type="bool",
        default=True,
    ),
    HyprOption(
        key="render:ctm_animation",
        section=("render",),
        name="ctm_animation",
        description=(
            "Whether to enable a fade animation for CTM changes (hyprsunset). 2 means 'auto' "
            "(Yes on everything but Nvidia)."
        ),
        type="int",
        default=2,
        min=0,
        max=2,
    ),
    HyprOption(
        key="render:cm_fs_passthrough",
        section=("render",),
        name="cm_fs_passthrough",
        description="Passthrough color settings for fullscreen apps when possible",
        type="int",
        default=2,
        min=0,
        max=2,
    ),
    HyprOption(
        key="render:cm_enabled",
        section=("render",),
        name="cm_enabled",
        description="Enable Color Management pipelines (requires restart to fully take effect)",
        type="bool",
        default=True,
    ),
    HyprOption(
        key="render:send_content_type",
        section=("render",),
        name="send_content_type",
        description=(
            "Report content type to allow monitor profile autoswitch (may result in a black s"
            "creen during the switch)"
        ),
        type="bool",
        default=True,
    ),
    HyprOption(
        key="render:cm_auto_hdr",
        section=("render",),
        name="cm_auto_hdr",
        description=(
            "Auto-switch to hdr mode when fullscreen app is in hdr, 0 - off, 1 - hdr, 2 - hdr"
            "edid (cm_fs_passthrough can switch to hdr even when this setting is off)"
        ),
        type="int",
        default=1,
        min=0,
        max=2,
    ),
    HyprOption(
        key="render:new_render_scheduling",
        section=("render",),
        name="new_render_scheduling",
        description=(
            "enable new render scheduling, which should improve FPS on underpowered devices. "
            "This does not add latency when your PC can keep up."
        ),
        type="bool",
        default=False,
    ),
    HyprOption(
        key="render:non_shader_cm",
        section=("render",),
        name="non_shader_cm",
        description=(
            "Enable CM without shader. 0 - disable, 1 - whenever possible, 2 - DS and passthr"
            "ough only, 3 - disable and ignore CM issues"
        ),
        type="choice",
        default=0,
        enum_values=("disable", "always", "ondemand", "ignore"),
        default_str="disable",
    ),
    HyprOption(
        key="render:cm_sdr_eotf",
        section=("render",),
        name="cm_sdr_eotf",
        description=(
            "Default transfer function for displaying SDR apps. default - Use default value ("
            "Gamma 2.2), gamma22 - Treat unspecified as Gamma 2.2, gamma22force - Treat unspe"
            "cified and sRGB as Gamma 2.2, srgb - Treat unspecified as sRGB"
        ),
        type="string",
        default="default",
    ),
    HyprOption(
        key="render:commit_timing_enabled",
        section=("render",),
        name="commit_timing_enabled",
        description="Enable commit timing proto. Requires restart",
        type="bool",
        default=True,
    ),
    HyprOption(
        key="render:icc_vcgt_enabled",
        section=("render",),
        name="icc_vcgt_enabled",
        description="Enable sending VCGT ramps to KMS with ICC profiles",
        type="bool",
        default=True,
    ),
    HyprOption(
        key="cursor:invisible",
        section=("cursor",),
        name="invisible",
        description="don't render cursors",
        type="bool",
        default=False,
    ),
    HyprOption(
        key="cursor:no_hardware_cursors",
        section=("cursor",),
        name="no_hardware_cursors",
        description="disables hardware cursors. Auto = disable when multi-gpu on nvidia",
        type="choice",
        default=0,
        enum_values=("Disabled", "Enabled", "Auto"),
        default_str="Disabled",
    ),
    HyprOption(
        key="cursor:no_break_fs_vrr",
        section=("cursor",),
        name="no_break_fs_vrr",
        description=(
            "disables scheduling new frames on cursor movement for fullscreen apps with VRR e"
            "nabled to avoid framerate spikes (may require no_hardware_cursors = true) 0 - of"
            "f, 1 - on, 2 - auto (on with content type 'game')"
        ),
        type="int",
        default=2,
        min=0,
        max=2,
    ),
    HyprOption(
        key="cursor:min_refresh_rate",
        section=("cursor",),
        name="min_refresh_rate",
        description=(
            "minimum refresh rate for cursor movement when no_break_fs_vrr is active. Set to "
            "minimum supported refresh rate or higher"
        ),
        type="int",
        default=24,
        min=10,
        max=500,
    ),
    HyprOption(
        key="cursor:hotspot_padding",
        section=("cursor",),
        name="hotspot_padding",
        description="the padding, in logical px, between screen edges and the cursor",
        type="int",
        default=1,
        min=0,
        max=20,
    ),
    HyprOption(
        key="cursor:inactive_timeout",
        section=("cursor",),
        name="inactive_timeout",
        description=(
            "in seconds, after how many seconds of cursor’s inactivity to hide it. Set to 0 f"
            "or never."
        ),
        type="int",
        default=0,
        min=0,
        max=20,
    ),
    HyprOption(
        key="cursor:no_warps",
        section=("cursor",),
        name="no_warps",
        description="if true, will not warp the cursor in many cases (focusing, keybinds, etc)",
        type="bool",
        default=False,
    ),
    HyprOption(
        key="cursor:persistent_warps",
        section=("cursor",),
        name="persistent_warps",
        description=(
            "When a window is refocused, the cursor returns to its last position relative to "
            "that window, rather than to the centre."
        ),
        type="bool",
        default=False,
    ),
    HyprOption(
        key="cursor:warp_on_change_workspace",
        section=("cursor",),
        name="warp_on_change_workspace",
        description=(
            "Move the cursor to the last focused window after changing the workspace. Options"
            ": 0 (Disabled), 1 (Enabled), 2 (Force - ignores cursor:no_warps option)"
        ),
        type="choice",
        default=0,
        enum_values=("Disabled", "Enabled", "Force"),
        default_str="Disabled",
    ),
    HyprOption(
        key="cursor:warp_on_toggle_special",
        section=("cursor",),
        name="warp_on_toggle_special",
        description=(
            "Move the cursor to the last focused window when toggling a special workspace. Op"
            "tions: 0 (Disabled), 1 (Enabled), 2 (Force - ignores cursor:no_warps option)"
        ),
        type="choice",
        default=0,
        enum_values=("Disabled", "Enabled", "Force"),
        default_str="Disabled",
    ),
    HyprOption(
        key="cursor:default_monitor",
        section=("cursor",),
        name="default_monitor",
        description=(
            "the name of a default monitor for the cursor to be set to on startup (see hyprct"
            "l monitors for names)"
        ),
        type="string",
        default="",
    ),
    HyprOption(
        key="cursor:zoom_factor",
        section=("cursor",),
        name="zoom_factor",
        description=(
            "the factor to zoom by around the cursor. Like a magnifying glass. Minimum 1.0 (m"
            "eaning no zoom)"
        ),
        type="float",
        default=1.0,
        min=1.0,
        max=10.0,
    ),
    HyprOption(
        key="cursor:zoom_rigid",
        section=("cursor",),
        name="zoom_rigid",
        description=(
            "whether the zoom should follow the cursor rigidly (cursor is always centered if "
            "it can be) or loosely"
        ),
        type="bool",
        default=False,
    ),
    HyprOption(
        key="cursor:zoom_disable_aa",
        section=("cursor",),
        name="zoom_disable_aa",
        description=(
            "If enabled, when zooming, no antialiasing will be used (zoom will be pixelated)"
        ),
        type="bool",
        default=False,
    ),
    HyprOption(
        key="cursor:zoom_detached_camera",
        section=("cursor",),
        name="zoom_detached_camera",
        description=(
            "Detaches the camera from the mouse when zoomed in, only ever moving to keep the "
            "mouse in view"
        ),
        type="bool",
        default=True,
    ),
    HyprOption(
        key="cursor:enable_hyprcursor",
        section=("cursor",),
        name="enable_hyprcursor",
        description="whether to enable hyprcursor support",
        type="bool",
        default=True,
    ),
    HyprOption(
        key="cursor:hide_on_key_press",
        section=("cursor",),
        name="hide_on_key_press",
        description="Hides the cursor when you press any key until the mouse is moved.",
        type="bool",
        default=False,
    ),
    HyprOption(
        key="cursor:hide_on_touch",
        section=("cursor",),
        name="hide_on_touch",
        description=(
            "Hides the cursor when the last input was a touch input until a mouse input is done."
        ),
        type="bool",
        default=True,
    ),
    HyprOption(
        key="cursor:hide_on_tablet",
        section=("cursor",),
        name="hide_on_tablet",
        description=(
            "Hides the cursor when the last input was a tablet input until a mouse input is done."
        ),
        type="bool",
        default=True,
    ),
    HyprOption(
        key="cursor:use_cpu_buffer",
        section=("cursor",),
        name="use_cpu_buffer",
        description=(
            "Makes HW cursors use a CPU buffer. Required on Nvidia to have HW cursors. 0 - of"
            "f, 1 - on, 2 - auto (nvidia only)"
        ),
        type="int",
        default=2,
        min=0,
        max=2,
    ),
    HyprOption(
        key="cursor:sync_gsettings_theme",
        section=("cursor",),
        name="sync_gsettings_theme",
        description=(
            "sync xcursor theme with gsettings, it applies cursor-theme and cursor-size on th"
            "eme load to gsettings making most CSD gtk based clients use same xcursor theme a"
            "nd size."
        ),
        type="bool",
        default=True,
    ),
    HyprOption(
        key="cursor:warp_back_after_non_mouse_input",
        section=("cursor",),
        name="warp_back_after_non_mouse_input",
        description=(
            "warp the cursor back to where it was after using a non-mouse input to move it, a"
            "nd then returning back to mouse."
        ),
        type="bool",
        default=False,
    ),
    HyprOption(
        key="ecosystem:no_update_news",
        section=("ecosystem",),
        name="no_update_news",
        description="disable the popup that shows up when you update hyprland to a new version.",
        type="bool",
        default=False,
    ),
    HyprOption(
        key="ecosystem:no_donation_nag",
        section=("ecosystem",),
        name="no_donation_nag",
        description="disable the popup that shows up twice a year encouraging to donate.",
        type="bool",
        default=False,
    ),
    HyprOption(
        key="ecosystem:enforce_permissions",
        section=("ecosystem",),
        name="enforce_permissions",
        description=(
            "whether to enable permission control (see https://wiki.hypr.land/Configuring/Per"
            "missions/)."
        ),
        type="bool",
        default=False,
    ),
    HyprOption(
        key="debug:overlay",
        section=("debug",),
        name="overlay",
        description="print the debug performance overlay. Disable VFR for accurate results.",
        type="bool",
        default=False,
    ),
    HyprOption(
        key="debug:damage_blink",
        section=("debug",),
        name="damage_blink",
        description="disable logging to a file",
        type="bool",
        default=False,
    ),
    HyprOption(
        key="debug:gl_debugging",
        section=("debug",),
        name="gl_debugging",
        description="enable OpenGL debugging and error checking, they hurt performance.",
        type="bool",
        default=False,
    ),
    HyprOption(
        key="debug:disable_logs",
        section=("debug",),
        name="disable_logs",
        description="disable logging to a file",
        type="bool",
        default=True,
    ),
    HyprOption(
        key="debug:disable_time",
        section=("debug",),
        name="disable_time",
        description="disables time logging",
        type="bool",
        default=True,
    ),
    HyprOption(
        key="debug:damage_tracking",
        section=("debug",),
        name="damage_tracking",
        description=(
            "redraw only the needed bits of the display. Do not change. (default: full - 2) m"
            "onitor - 1, none - 0"
        ),
        type="int",
        default=2,
        min=0,
        max=2,
    ),
    HyprOption(
        key="debug:enable_stdout_logs",
        section=("debug",),
        name="enable_stdout_logs",
        description="enables logging to stdout",
        type="bool",
        default=False,
    ),
    HyprOption(
        key="debug:manual_crash",
        section=("debug",),
        name="manual_crash",
        description="set to 1 and then back to 0 to crash Hyprland.",
        type="int",
        default=0,
        min=0,
        max=1,
    ),
    HyprOption(
        key="debug:suppress_errors",
        section=("debug",),
        name="suppress_errors",
        description="if true, do not display config file parsing errors.",
        type="bool",
        default=False,
    ),
    HyprOption(
        key="debug:disable_scale_checks",
        section=("debug",),
        name="disable_scale_checks",
        description=(
            "disables verification of the scale factors. Will result in pixel alignment and r"
            "ounding errors."
        ),
        type="bool",
        default=False,
    ),
    HyprOption(
        key="debug:error_limit",
        section=("debug",),
        name="error_limit",
        description="limits the number of displayed config file parsing errors.",
        type="int",
        default=5,
        min=0,
        max=20,
    ),
    HyprOption(
        key="debug:error_position",
        section=("debug",),
        name="error_position",
        description="sets the position of the error bar. top - 0, bottom - 1",
        type="int",
        default=0,
        min=0,
        max=1,
    ),
    HyprOption(
        key="debug:colored_stdout_logs",
        section=("debug",),
        name="colored_stdout_logs",
        description="enables colors in the stdout logs.",
        type="bool",
        default=True,
    ),
    HyprOption(
        key="debug:log_damage",
        section=("debug",),
        name="log_damage",
        description="enables logging the damage.",
        type="bool",
        default=False,
    ),
    HyprOption(
        key="debug:pass",
        section=("debug",),
        name="pass",
        description="enables render pass debugging.",
        type="bool",
        default=False,
    ),
    HyprOption(
        key="debug:full_cm_proto",
        section=("debug",),
        name="full_cm_proto",
        description="claims support for all cm proto features (requires restart)",
        type="bool",
        default=False,
    ),
    HyprOption(
        key="debug:ds_handle_same_buffer",
        section=("debug",),
        name="ds_handle_same_buffer",
        description="Special case for DS with unmodified buffer",
        type="bool",
        default=True,
    ),
    HyprOption(
        key="debug:ds_handle_same_buffer_fifo",
        section=("debug",),
        name="ds_handle_same_buffer_fifo",
        description="Special case for DS with unmodified buffer unlocks fifo",
        type="bool",
        default=True,
    ),
    HyprOption(
        key="debug:fifo_pending_workaround",
        section=("debug",),
        name="fifo_pending_workaround",
        description="Fifo workaround for empty pending list",
        type="bool",
        default=False,
    ),
    HyprOption(
        key="debug:render_solitary_wo_damage",
        section=("debug",),
        name="render_solitary_wo_damage",
        description="Render solitary window with empty damage",
        type="bool",
        default=False,
    ),
    HyprOption(
        key="layout:single_window_aspect_ratio",
        section=("layout",),
        name="single_window_aspect_ratio",
        description=(
            "If specified, whenever only a single window is open, it will be coerced into the"
            " specified aspect ratio.  Ignored if the y-value is zero."
        ),
        type="vec2",
        default=(0.0, 0.0),
        default_min=(0.0, 0.0),
        default_max=(1000.0, 1000.0),
    ),
    HyprOption(
        key="layout:single_window_aspect_ratio_tolerance",
        section=("layout",),
        name="single_window_aspect_ratio_tolerance",
        description=(
            "Minimum distance for single_window_aspect_ratio to take effect, in fractions of "
            "the monitor's size."
        ),
        type="float",
        default=0.1,
        min=0.0,
        max=1.0,
    ),
    HyprOption(
        key="dwindle:pseudotile",
        section=("dwindle",),
        name="pseudotile",
        description=(
            "enable pseudotiling. Pseudotiled windows retain their floating size when tiled."
        ),
        type="bool",
        default=False,
    ),
    HyprOption(
        key="dwindle:force_split",
        section=("dwindle",),
        name="force_split",
        description=(
            "0 -> split follows mouse, 1 -> always split to the left (new = left or top) 2 ->"
            " always split to the right (new = right or bottom)"
        ),
        type="choice",
        default=0,
        enum_values=("follow mouse", "left or top", "right or bottom"),
        default_str="follow mouse",
    ),
    HyprOption(
        key="dwindle:preserve_split",
        section=("dwindle",),
        name="preserve_split",
        description=(
            "if enabled, the split (side/top) will not change regardless of what happens to t"
            "he container."
        ),
        type="bool",
        default=False,
    ),
    HyprOption(
        key="dwindle:smart_split",
        section=("dwindle",),
        name="smart_split",
        description=(
            "if enabled, allows a more precise control over the window split direction based "
            "on the cursor's position. The window is conceptually divided into four triangles"
            ", and cursor's triangle determines the split direction. This feature also turns "
            "on preserve_split."
        ),
        type="bool",
        default=False,
    ),
    HyprOption(
        key="dwindle:smart_resizing",
        section=("dwindle",),
        name="smart_resizing",
        description=(
            "if enabled, resizing direction will be determined by the mouse's position on the"
            " window (nearest to which corner). Else, it is based on the window's tiling posi"
            "tion."
        ),
        type="bool",
        default=True,
    ),
    HyprOption(
        key="dwindle:permanent_direction_override",
        section=("dwindle",),
        name="permanent_direction_override",
        description=(
            "if enabled, makes the preselect direction persist until either this mode is turn"
            "ed off, another direction is specified, or a non-direction is specified (anythin"
            "g other than l,r,u/t,d/b)"
        ),
        type="bool",
        default=False,
    ),
    HyprOption(
        key="dwindle:special_scale_factor",
        section=("dwindle",),
        name="special_scale_factor",
        description="specifies the scale factor of windows on the special workspace [0 - 1]",
        type="float",
        default=1.0,
        min=0.0,
        max=1.0,
    ),
    HyprOption(
        key="dwindle:split_width_multiplier",
        section=("dwindle",),
        name="split_width_multiplier",
        description="specifies the auto-split width multiplier",
        type="float",
        default=1.0,
        min=0.1,
        max=3.0,
    ),
    HyprOption(
        key="dwindle:use_active_for_splits",
        section=("dwindle",),
        name="use_active_for_splits",
        description="whether to prefer the active window or the mouse position for splits",
        type="bool",
        default=True,
    ),
    HyprOption(
        key="dwindle:default_split_ratio",
        section=("dwindle",),
        name="default_split_ratio",
        description="the default split ratio on window open. 1 means even 50/50 split. [0.1 - 1.9]",
        type="float",
        default=1.0,
        min=0.1,
        max=1.9,
    ),
    HyprOption(
        key="dwindle:split_bias",
        section=("dwindle",),
        name="split_bias",
        description=(
            "specifies which window will receive the split ratio. 0 -> directional (the top o"
            "r left window), 1 -> the current window"
        ),
        type="choice",
        default=0,
        enum_values=("directional", "current"),
        default_str="directional",
    ),
    HyprOption(
        key="dwindle:precise_mouse_move",
        section=("dwindle",),
        name="precise_mouse_move",
        description=(
            "if enabled, bindm movewindow will drop the window more precisely depending on wh"
            "ere your mouse is. This feature also turns on preserve_split."
        ),
        type="bool",
        default=False,
    ),
    HyprOption(
        key="master:allow_small_split",
        section=("master",),
        name="allow_small_split",
        description="enable adding additional master windows in a horizontal split style",
        type="bool",
        default=False,
    ),
    HyprOption(
        key="master:special_scale_factor",
        section=("master",),
        name="special_scale_factor",
        description="the scale of the special workspace windows. [0.0 - 1.0]",
        type="float",
        default=1.0,
        min=0.0,
        max=1.0,
    ),
    HyprOption(
        key="master:mfact",
        section=("master",),
        name="mfact",
        description=(
            "the size as a percentage of the master window, for example `mfact = 0.70` would "
            "mean 70% of the screen will be the master window, and 30% the slave [0.0 - 1.0]"
        ),
        type="float",
        default=0.55,
        min=0.0,
        max=1.0,
    ),
    HyprOption(
        key="master:new_status",
        section=("master",),
        name="new_status",
        description=(
            "`master`: new window becomes master; `slave`: new windows are added to slave sta"
            "ck; `inherit`: inherit from focused window"
        ),
        type="string",
        default="slave",
    ),
    HyprOption(
        key="master:new_on_top",
        section=("master",),
        name="new_on_top",
        description="whether a newly open window should be on the top of the stack",
        type="bool",
        default=False,
    ),
    HyprOption(
        key="master:new_on_active",
        section=("master",),
        name="new_on_active",
        description=(
            "`before`, `after`: place new window relative to the focused window; `none`: plac"
            "e new window according to the value of `new_on_top`."
        ),
        type="string",
        default="none",
    ),
    HyprOption(
        key="master:orientation",
        section=("master",),
        name="orientation",
        description=(
            "default placement of the master area, can be left, right, top, bottom or center"
        ),
        type="string",
        default="left",
    ),
    HyprOption(
        key="master:slave_count_for_center_master",
        section=("master",),
        name="slave_count_for_center_master",
        description=(
            "when using orientation=center, make the master window centered only when at leas"
            "t this many slave windows are open. (Set 0 to always_center_master)"
        ),
        type="int",
        default=2,
        min=0,
        max=10,
    ),
    HyprOption(
        key="master:center_master_fallback",
        section=("master",),
        name="center_master_fallback",
        description=(
            "Set fallback for center master when slaves are less than slave_count_for_center_"
            "master, can be left ,right ,top ,bottom"
        ),
        type="string",
        default="left",
    ),
    HyprOption(
        key="master:center_ignores_reserved",
        section=("master",),
        name="center_ignores_reserved",
        description="centers the master window on monitor ignoring reserved areas",
        type="bool",
        default=False,
    ),
    HyprOption(
        key="master:smart_resizing",
        section=("master",),
        name="smart_resizing",
        description=(
            "if enabled, resizing direction will be determined by the mouse's position on the"
            " window (nearest to which corner). Else, it is based on the window's tiling posi"
            "tion."
        ),
        type="bool",
        default=True,
    ),
    HyprOption(
        key="master:drop_at_cursor",
        section=("master",),
        name="drop_at_cursor",
        description=(
            "when enabled, dragging and dropping windows will put them at the cursor position"
            ". Otherwise, when dropped at the stack side, they will go to the top/bottom of t"
            "he stack depending on new_on_top."
        ),
        type="bool",
        default=True,
    ),
    HyprOption(
        key="master:always_keep_position",
        section=("master",),
        name="always_keep_position",
        description=(
            "whether to keep the master window in its configured position when there are no s"
            "lave windows"
        ),
        type="bool",
        default=False,
    ),
    HyprOption(
        key="scrolling:fullscreen_on_one_column",
        section=("scrolling",),
        name="fullscreen_on_one_column",
        description=(
            "when enabled, a single column on a workspace will always span the entire screen."
        ),
        type="bool",
        default=True,
    ),
    HyprOption(
        key="scrolling:column_width",
        section=("scrolling",),
        name="column_width",
        description="the default width of a column, [0.1 - 1.0].",
        type="float",
        default=0.5,
        min=0.1,
        max=1.0,
    ),
    HyprOption(
        key="scrolling:focus_fit_method",
        section=("scrolling",),
        name="focus_fit_method",
        description="When a column is focused, what method should be used to bring it into view",
        type="choice",
        default=0,
        enum_values=("center", "fit"),
        default_str="center",
    ),
    HyprOption(
        key="scrolling:follow_focus",
        section=("scrolling",),
        name="follow_focus",
        description=(
            "when a window is focused, should the layout move to bring it into view automatically"
        ),
        type="bool",
        default=False,
    ),
    HyprOption(
        key="scrolling:follow_min_visible",
        section=("scrolling",),
        name="follow_min_visible",
        description=(
            "when a window is focused, require that at least a given fraction of it is visibl"
            "e for focus to follow"
        ),
        type="float",
        default=0.4,
        min=0.0,
        max=1.0,
    ),
    HyprOption(
        key="scrolling:explicit_column_widths",
        section=("scrolling",),
        name="explicit_column_widths",
        description="A comma-separated list of preconfigured widths for colresize +conf/-conf",
        type="string",
        default="0.333, 0.5, 0.667, 1.0",
    ),
    HyprOption(
        key="scrolling:direction",
        section=("scrolling",),
        name="direction",
        description="Direction in which new windows appear and the layout scrolls",
        type="choice",
        default=0,
        enum_values=("right", "left", "down", "up"),
        default_str="right",
    ),
    HyprOption(
        key="quirks:prefer_hdr",
        section=("quirks",),
        name="prefer_hdr",
        description="Prefer HDR mode. 0 - off, 1 - always, 2 - gamescope only",
        type="int",
        default=0,
        min=0,
        max=2,
    ),
    HyprOption(
        key="quirks:skip_non_kms_dmabuf_formats",
        section=("quirks",),
        name="skip_non_kms_dmabuf_formats",
        description="Do not report dmabuf formats which cannot be imported into KMS",
        type="bool",
        default=True,
    ),
)
